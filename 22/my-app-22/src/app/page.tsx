import useDestination from "../../hooks/useDestination";

export default function Home() {
  return (
    <section className="relative z-90
        flex flex-col
        w-full min-h-svh">
            <div className="flex flex-col md:flex-row lg:flex-row 
            justify-end md:justify-between lg:justify-between
            items-center md:items-end lg:items-end gap-6
            w-full md:max-w-240 lg:max-w-7xl mx-auto
            flex-1 py-6 md:py-10 lg:py-16 px-5 md:px-8 lg:px-12">
            </div>
    </section>
  );
}
